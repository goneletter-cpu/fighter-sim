很好的问题，这里我把“脚本 → JSBSim 属性”的映射讲清楚，并解释 fcs/*、controls/*、propulsion/* 三个命名空间各自的含义和在你项目中的作用。

1) 三个命名空间的含义（概念层）
fcs/*（Flight Control System）
这是 飞控命令层，相当于“我们给飞控系统的命令”。
*-cmd-norm 一般是 归一化指令，范围通常是 [-1, 1] 或 [0, 1]。

在你的代码里：

fcs/aileron-cmd-norm
fcs/elevator-cmd-norm
fcs/rudder-cmd-norm
fcs/throttle-cmd-norm
fcs/flap-cmd-norm
fcs/gear-cmd-norm
controls/*
这是 驾驶舱控件层，相当于“驾驶员真实操纵杆/油门杆的位置”。
某些机型（如 c172p）的系统逻辑更依赖 controls/*，也就是说：

即使你写了 fcs/*，也可能要同步 controls/* 才生效。
在你代码里被写入的关键 controls：

controls/engines/engine[0]/throttle
controls/engines/current-engine/throttle
controls/engines/throttle-all
controls/engines/current-engine/mixture
controls/engines/engine[0]/mixture
controls/switches/master-bat / master-alt
controls/switches/magnetos / starter
propulsion/*
这是 发动机物理层，直接影响发动机工作状态/动力输出。
通常只有系统逻辑或初始化时会写，运行中常读状态。

你现在用到的：

propulsion/engine[0]/set-running
propulsion/starter_cmd
propulsion/cutoff_cmd
propulsion/engine[0]/rpm（日志读取）
2) 脚本如何映射到 JSBSim 属性（你现在的实现）
当前脚本格式（flight_script.txt）
你脚本 columns: 里写的是这些列名，它们直接映射到 Phase 结构：

fcs/throttle-cmd-norm → Phase.throttle
fcs/elevator-cmd-norm → Phase.elevator
fcs/aileron-cmd-norm → Phase.aileron
fcs/rudder-cmd-norm → Phase.rudder
fcs/flap-cmd-norm → Phase.flap
fcs/gear-cmd-norm → Phase.gear
对应代码位置
/Volumes/ThePan/DocumentsOnExternal/workspace/fighter-sim/src/main.cpp
load_script() 内这一段会把列名解析后写入 Phase。

目标值模式（PID）
当 columns 里出现 target_*：

target_speed_mps
target_climb_mps
target_heading_deg
就会走 PID 控制逻辑，在运行时用 PID 输出生成 fcs/* 输入，然后通过 jsbsim.set_controls() 写进去。

3) 你的项目中实际写入 JSBSim 的路径
代码调用 jsbsim.set_controls(ctrl) 时：

直接写 fcs/*
同时写 controls/engines/*（油门）
所以 脚本里写 fcs/throttle-cmd-norm 最终会落到这两个体系里。
4) 你改脚本时的关键规则
如果你写的是 fcs/
就相当于直接控制飞行面（命令层）。
如果你写的是 controls/
等于直接推驾驶杆/油门杆位置。
propulsion/
通常不直接放到脚本里，除非你想做发动机异常/强制启动。
如果你希望进一步支持脚本里直接写 controls/* 或 propulsion/*
我可以帮你把解析器扩展成“任意 JSBSim 属性 → 直接写入”，这样脚本就不需要固定列名，只要 property, value 即可。
你要不要往这一步走？