明白，这三块我给你拆到“可以直接改”的粒度，全部指到具体文件与位置。

JSBSim 启动流程与关键属性
入口：/Volumes/ThePan/DocumentsOnExternal/workspace/fighter-sim/src/jsbsim_adapter.cpp 的 JSBSimAdapter::init(...)

流程概览（按顺序）

设置模型路径
根据 fgdata: / fgaddon: 前缀设置 root 和 Aircraft/Engines/Systems 路径。
LoadModel 加载机型
调用 fdm_->LoadModel(model_name, add_model_to_path)
设置初始状态
使用 GetIC() 设置高度、速度、姿态、风、地面高程等
关键属性：
ic->SetAltitudeASLFtIC(...)
ic->SetVNorthFpsIC(...) / SetVEastFpsIC(...) / SetVDownFpsIC(...)
ic->SetPhiRadIC(...) / SetThetaRadIC(...) / SetPsiRadIC(...)
设置发动机与电气/点火相关属性
当前你这套里已经写了这些关键属性：
电源主开关
/controls/switches/master-bat = 1
/controls/switches/master-alt = 1
/controls/circuit-breakers/master = 1
点火/起动
/controls/switches/magnetos = 3
/controls/switches/starter = 1（RunIC 之前）
/controls/engines/current-engine/starter = 1
/controls/engines/engine[0]/starter = 1
混合比
/controls/engines/current-engine/mixture = 1
/controls/engines/engine[0]/mixture = 1
其它
/propulsion/engine[0]/set-running = 1
/propulsion/starter_cmd = 1
/propulsion/cutoff_cmd = 0
RunIC() 后释放 starter
防止一直打火：
/controls/switches/starter = 0
/controls/engines/current-engine/starter = 0
/controls/engines/engine[0]/starter = 0
调试建议

在 last_flight.txt 里看 eng_rpm
当前优先使用 propulsion/engine[0]/rpm，如果为 0 再看 engines/active-engine/rpm
相关读取在 src/main.cpp 的日志输出块
flight_script.txt 的解析逻辑细节
入口：/Volumes/ThePan/DocumentsOnExternal/workspace/fighter-sim/src/main.cpp
函数：static ScriptConfig load_script(const std::string& path)

文件格式规则

支持 key value 或 key: value 两种写法
被识别的 key：
run_mode
model
models（逗号分隔）
initial_alt_m
initial_speed_mps
columns: 决定数据列的含义
你现在用的是：
duration_s
target_speed_mps
target_climb_mps
target_heading_deg
fcs/flap-cmd-norm
fcs/gear-cmd-norm
columns 名字会被 lower_copy() 变成小写比较
数据行
可以是 CSV 或空格分隔
每一行会生成一个 Phase
如果列里出现 target_*，就会 p.has_targets = true 并开启 cfg.use_pid = true
没有 columns 时的默认顺序
duration_s, throttle, elevator, aileron, rudder, flap, gear
你要修改解析时，关注这几个辅助函数

split_csv(...)
split_ws(...)
lower_copy(...)
PID 入口 / 参数 / 调参点
PID 实现：
/Volumes/ThePan/DocumentsOnExternal/workspace/fighter-sim/src/pid.cpp
类接口：
/Volumes/ThePan/DocumentsOnExternal/workspace/fighter-sim/include/pid.h

PID 实例创建位置
/Volumes/ThePan/DocumentsOnExternal/workspace/fighter-sim/src/main.cpp
在 main 里有：

PID speed_pid(...)
PID climb_pid(...)
PID heading_pid(...)
当前 PID 参数

speed_pid(0.06f, 0.005f, 0.02f, 0.6f)
climb_pid(0.20f, 0.02f, 0.06f, 0.4f)
heading_pid(0.02f, 0.000f, 0.01f, 0.35f)
控制入口位置
在主物理步循环里（while (accumulator >= SIM_DT)），当 script.use_pid || phase.has_targets 为真时：

速度控制 → ctrl.throttle
爬升控制 → ctrl.elevator
航向控制 → ctrl.rudder
关键计算方式

速度误差：target_speed_mps - |velocity|
爬升误差：target_climb_mps - state.velocity.y
航向误差：shortest_angle_deg(target_heading_deg, current_heading_deg)
PID 的饱和限制

output_limit_ 在构造函数里最后一个参数
输出再被 clamp 到舵面范围（例如 [-0.6, 0.6]）
重置时机

每个 phase 切换时 reset
每次 model 切换也 reset
调参建议（简要）

速度 PID：
Kp 大一点加快加速
Ki 太高会超调，谨慎加
爬升 PID：
Kp 稍高，Ki 少量，Kd 可抑制抖动
航向 PID：
先只调 Kp 和 Kd，Ki 通常不需要
如果你希望我把 PID 参数移动到 flight_script.txt（每个 phase 自带 PID 参数），我也可以直接改，方便你自己调。